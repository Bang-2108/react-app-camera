import React, {useState} from "react";
import GetData from "./GetData";

export default function GetDataFunction() {
    const productList = () => {
        const [products, setProducts] = useState(productsData)
    }
}